# repA-code

Demo code repository consuming `repB-res` metadata and downloading only the
resource selected by `resource.lock`.

```sh
./tools/ofa sync
./tools/ofa build
./tools/ofa run
./tools/ofa switch <branch>
```

